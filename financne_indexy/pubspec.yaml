name: financne_indexy
description: Sledovanie finančných indexov s grafmi
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  fl_chart: ^0.68.0
  flutter_local_notifications: ^17.2.1
  intl: ^0.19.0
  shared_preferences: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/mcs_bf_feeds/usa.json
    - assets/mcs_bf_feeds/world.json
    - assets/mcs_bf_feeds/europe.json
